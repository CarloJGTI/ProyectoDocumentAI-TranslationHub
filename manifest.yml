---
applications:
  - name: aiservices-app         # El nombre que tendrá en BTP
    path: .
    memory: 256M                 # Memoria asignada
    buildpack: nodejs_buildpack
    random-route: true           # Crea una URL única (ej: aiservices-app-random.cfapps...)
    command: node srv/server.js  # Comando de inicio
    services:
      - default_sap-document-information-extraction            
      - default_document-translation
    env:
      SANDBOX-API-KEY: "NQSRAC0Pg4WzlokOGjvFM5bOozDlcilJ"
      OPTIMIZE_MEMORY: true