---
applications:
  - name: aiservices-app         
    path: .
    memory: 256M                 
    buildpack: nodejs_buildpack
    random-route: true           
    command: node srv/server.js  
    services:
      - default_sap-document-information-extraction
    # -----------------------------------------------------------
    # VARIABLES DE ENTORNO
    # -----------------------------------------------------------
    env:
      ILOVEPDF_PUBLIC_KEY: "project_public_key (COMPLETAR)"
      SANDBOX-API-KEY: "NQSRAC0Pg4WzlokOGjvFM5bOozDlcilJ (EN CASO DE FALLBACK)"
      OPTIMIZE_MEMORY: true